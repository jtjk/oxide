import {
  debugWriter
} from '../../../helpers/debug-writer';
import { module, test } from 'qunit';

module('DebugWriterHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = debugWriter(42);
  assert.ok(result);
});
